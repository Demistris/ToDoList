<div id="sortableList">
    @foreach(var item in Items)
    {
        <div class="toDo-item no-select" @key="item.Id">
            @if(SortableItem != null)
            {
                @SortableItem(item);
            }
        </div>
    }
</div>